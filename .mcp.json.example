{
  "$schema": "https://json.schemastore.org/mcp.json",
  "mcpServers": {
    "fulltech-memory": {
      "type": "sse",
      "url": "${FULLTECH_MEMORY_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${FULLTECH_MEMORY_API_KEY}"
      }
    }
  }
}
